.DS_Store
Thumbs.db
desktop.ini
*.tmp
*.swp
.vscode/
.idea/

# Raw transit downloads — only the slimmed .min.geojson files belong in repo.
web/data/transit/subway-lines.geojson
web/data/transit/subway-stations.geojson
web/data/transit/bus-routes.geojson

# Secrets — used by deploy_portfolio.py to encrypt the published listings blob.
# Must never be committed: the password lives only here on the brain side.
.env.local
.env
